{
  "name": "suniemi",
  "version": "2.0.0",
  "description": "A stylish terminal wizard that generates a README.md by scanning your project - no AI, no network calls, no paid services.",
  "type": "module",
  "main": "bin/suniemi.js",
  "bin": {
    "suniemi": "./bin/suniemi.js"
  },
  "scripts": {
    "check": "node --check bin/suniemi.js && node --check src/scan.js && node --check src/badges.js && node --check src/render.js && node --check src/wizard.js && node --check scripts/smoke.js",
    "smoke": "node scripts/smoke.js"
  },
  "keywords": [
    "readme",
    "cli",
    "generator",
    "tui",
    "terminal"
  ],
  "author": "Riley Jordan",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18.3.0"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.0",
    "gradient-string": "^3.0.0",
    "picocolors": "^1.0.0"
  }
}
