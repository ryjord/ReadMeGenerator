{
  "name": "readme-gen",
  "version": "2.0.0",
  "description": "A stylish terminal wizard that generates a README.md by scanning your project - no AI, no network calls, no paid services.",
  "type": "module",
  "main": "bin/readme-gen.js",
  "bin": {
    "readme-gen": "./bin/readme-gen.js"
  },
  "scripts": {
    "check": "node --check bin/readme-gen.js && node --check src/scan.js && node --check src/badges.js && node --check src/render.js && node --check src/wizard.js && node --check scripts/smoke.js",
    "smoke": "node scripts/smoke.js"
  },
  "keywords": [
    "readme",
    "cli",
    "generator",
    "tui",
    "terminal"
  ],
  "author": "Riley Jordan",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18.3.0"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.0",
    "gradient-string": "^3.0.0",
    "picocolors": "^1.0.0"
  }
}
